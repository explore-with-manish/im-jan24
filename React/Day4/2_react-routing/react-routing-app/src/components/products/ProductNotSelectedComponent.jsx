import React from 'react';

const ProductNotSelectedComponent = () => {
    return (
        <h2 className="text-warning">Please select a product</h2>
    );
};

export default ProductNotSelectedComponent;